# msg-hub — plan wdrożenia

> **Tryb wykonania:** trzy fazy, przegląd po każdej. NIE po każdym zadaniu.
> Wykonawca realizuje wszystkie zadania fazy jednym ciągiem, po czym zatrzymuje się
> na przegląd z operatorem. Kroki mają checkboxy (`- [ ]`) do śledzenia postępu.

**Cel:** Aplikacja desktopowa na Windows z trzema kontami komunikatorów w izolowanych sesjach oraz makrami tekstowymi z załącznikami.

**Architektura:** Jedno okno Electron. Każde konto to `WebContentsView` z własną partycją sesji (`persist:<id>`), co daje pełną izolację ciasteczek i magazynu. Makra trafiają do czatu wyłącznie przez schowek systemowy — aplikacja nigdy nie wysyła wiadomości ani nie manipuluje wnętrzem stron.

**Stack:** Node v26.2.0, Electron 43.4.1, Vitest 4.1.11 (logika), Playwright 1.62.1 z `_electron` (integracja), electron-builder 26.15.3. Czysty JavaScript ESM — bez TypeScriptu i bez bundlera.

**Spec:** `docs/superpowers/specs/2026-08-23-msg-hub-design.md` — czytaj razem z tym planem.

---

## Start w świeżej sesji

Ta sekcja istnieje, bo plan bywa wykonywany po utracie kontekstu rozmowy.

**Gdzie:** `C:\Users\marek\src\msg-hub`, gałąź `main`. Repo jest lokalne, bez zdalnego.

**Stan na starcie:** repo zawiera wyłącznie `docs/` — spec i ten plan. Żadnego kodu, `package.json` też jeszcze nie ma. Zadanie A1 tworzy wszystko od zera.

**Co już rozstrzygnięto, żeby tego nie powtarzać:**

- Aplikacja jest **lokalna na `hp-x360-win`**. Wersja serwerowa i dostęp z laptopa służbowego zostały wycofane — spec, sekcja 12, tłumaczy dlaczego. Nie wracaj do tych wariantów.
- **Etap 0 wykonany 2026-08-23 z wynikiem pozytywnym**: WhatsApp Web przyjmuje wklejenie pliku ze schowka Windows. Zweryfikowano na `.mp4` 10,23 MB i `.pdf` 0,74 MB w Edge. Dlatego załączniki idą przez schowek, a mechanizm podstawiania pliku w formularzu **nie wchodzi do projektu**.
- Znaleziony przy tym quirk: `Set-Clipboard -LiteralPath` i `Get-Clipboard -Format` istnieją **tylko w Windows PowerShell 5.1**. PowerShell 7 ich nie ma.

**Weryfikacja stanu przed startem:**

```bash
cd /c/Users/marek/src/msg-hub && git log --oneline && git status --short
```

---

## Ograniczenia globalne

Obowiązują w każdym zadaniu. Wynikają z sekcji 7 specu i z audytu, który poprzedził projekt.

- **Zakaz `wa-js`, WPPConnect, Baileys i bibliotek pokrewnych.** Żadnego dostępu do wewnętrznych funkcji WhatsApp Web. Strony ładujemy takie, jakie serwuje Meta.
- **Zakaz automatycznej wysyłki.** Żadna ścieżka kodu nie może wywołać wysłania wiadomości. Makro wstawia treść i się zatrzymuje.
- **Wstawianie wyłącznie przez schowek.** Bez manipulacji DOM stron, także przy załącznikach.
- **`User-Agent` musi mieć usunięty znacznik Electrona** — inaczej WhatsApp Web odmówi obsługi.
- **`CF_HDROP` ustawiamy przez `powershell.exe`, nigdy przez `pwsh`.**
- **`id` konta jest niezmienne** — służy jako nazwa partycji; zmiana oznacza utratę sesji.
- **Limit pojedynczego załącznika: 100 MB.**
- **Nazwy plików, funkcji i kluczy JSON po polsku** — spójnie ze specyfikacją.
- Platforma docelowa: Windows 11, wyłącznie `hp-x360-win`.

---

## Struktura plików

```
msg-hub/
├── package.json
├── playwright.config.js
├── src/
│   ├── glowny/
│   │   ├── main.js            punkt wejścia; składa resztę, sam nie zawiera logiki
│   │   ├── konta.js           rejestr kont: odczyt, zapis, walidacja        [faza A]
│   │   ├── widoki.js          WebContentsView per konto, partycje, UA       [faza A]
│   │   ├── powloka.js         układ okna, autostart                         [faza A]
│   │   ├── most.js            kanały IPC — rośnie w każdej fazie
│   │   ├── makra.js           magazyn makr i załączników              [fazy B i C]
│   │   ├── wstawianie.js      schowek i wklejanie tekstu                     [faza B]
│   │   └── schowek-pliku.js   CF_HDROP przez powershell.exe                  [faza C]
│   ├── preload/
│   │   └── preload.js         mostek do renderera — rośnie w każdej fazie
│   └── renderer/
│       ├── index.html         rośnie w każdej fazie
│       ├── renderer.js        rośnie w każdej fazie
│       └── styl.css
└── tests/
    ├── konta.test.js
    ├── powloka.test.js
    ├── widoki.test.js
    ├── makra.test.js
    ├── wstawianie.test.js
    ├── granice.test.js        egzekucja reguł 7.1 i 7.2 specu
    ├── schowek-pliku.test.js
    └── e2e/
        ├── start.spec.js
        └── izolacja.spec.js   dowód izolacji sesji — sedno produktu
```

---

## Mapa trzech faz

| Faza | Zadania | Co masz po jej zakończeniu |
|---|---|---|
| **A — Hub kont** | A1–A6 | Działającą aplikację: trzy konta w zakładkach, izolowane sesje, powiadomienia. **Już zastępuje płatną apkę.** |
| **B — Makra tekstowe** | B1–B4 | Panel makr pod `Ctrl+;`, edytor z podglądem formatowania WhatsApp, wstawianie tekstu do czatu. |
| **C — Załączniki i dystrybucja** | C1–C4 | Makra z plikami PDF i mp4, przenośny `.exe` z autostartem. |

Każda faza kończy się aplikacją, którą można uruchomić i ocenić ręcznie — dlatego interfejs i IPC rosną przyrostowo, zamiast powstawać na końcu.

---

# FAZA A — Hub kont

Cel fazy: `npm start` otwiera okno, dodajesz trzy konta, logujesz się kodem QR, przełączasz zakładkami, dostajesz powiadomienia. Izolacja sesji udowodniona testem.

---

### Zadanie A1: Szkielet projektu i puste okno

**Pliki:**
- Utwórz: `package.json`, `.gitignore`, `playwright.config.js`, `src/glowny/main.js`
- Test: `tests/e2e/start.spec.js`

**Interfejsy:**
- Konsumuje: nic
- Produkuje: skrypty `npm start`, `npm test`, `npm run test:e2e`

- [ ] **Krok 1: Utwórz `package.json`**

```json
{
  "name": "msg-hub",
  "version": "0.1.0",
  "description": "Hub komunikatorow z izolowanymi sesjami kont",
  "main": "src/glowny/main.js",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "electron .",
    "test": "vitest run",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "electron": "43.4.1",
    "vitest": "4.1.11"
  }
}
```

- [ ] **Krok 2: Utwórz `.gitignore`**

```gitignore
node_modules/
dist/
test-results/
playwright-report/
```

- [ ] **Krok 3: Utwórz `playwright.config.js`**

```javascript
export default {
  testDir: './tests/e2e',
  timeout: 30000,
  fullyParallel: false,
  workers: 1,
}
```

- [ ] **Krok 4: Zainstaluj zależności**

```bash
npm install
```

- [ ] **Krok 5: Napisz test startu**

Plik `tests/e2e/start.spec.js`:

```javascript
import { test, expect, _electron as electron } from '@playwright/test'

test('aplikacja startuje i otwiera jedno okno', async () => {
  const aplikacja = await electron.launch({ args: ['.'] })
  const okno = await aplikacja.firstWindow()
  expect(await okno.title()).toBe('msg-hub')
  await aplikacja.close()
})
```

- [ ] **Krok 6: Uruchom test — musi zawieść**

```bash
npm run test:e2e
```

Oczekiwane: FAIL — brak `src/glowny/main.js`.

- [ ] **Krok 7: Napisz `src/glowny/main.js`**

```javascript
import { app, BrowserWindow } from 'electron'

function utworzOkno() {
  const okno = new BrowserWindow({
    width: 1280,
    height: 800,
    title: 'msg-hub',
    backgroundColor: '#111b21',
  })
  okno.setTitle('msg-hub')
  return okno
}

app.whenReady().then(() => {
  utworzOkno()
})

app.on('window-all-closed', () => {
  app.quit()
})
```

- [ ] **Krok 8: Uruchom test — musi przejść**

```bash
npm run test:e2e
```

Oczekiwane: PASS, 1 test.

- [ ] **Krok 9: Commit**

```bash
git add package.json .gitignore playwright.config.js src tests
git commit -m "feat: szkielet Electron z pustym oknem i testem startu"
```

---

### Zadanie A2: Rejestr kont

**Pliki:**
- Utwórz: `src/glowny/konta.js`
- Test: `tests/konta.test.js`

**Interfejsy:**
- Konsumuje: nic
- Produkuje:
  - `WERSJA_SCHEMATU` → `number` (1)
  - `PLATFORMY` → `{whatsapp: {url, nazwaDomyslna}, messenger: {url, nazwaDomyslna}}`
  - `waliduj(konto)` → `string[]` (pusta lista = poprawne)
  - `utworzIdKonta(nazwa, istniejaceId)` → `string`
  - `async wczytajKonta(sciezkaPliku)` → `Promise<{wersja: number, konta: Konto[]}>`
  - `async zapiszKonta(sciezkaPliku, konta)` → `Promise<void>`
  - `Konto` = `{id: string, nazwa: string, platforma: 'whatsapp'|'messenger', url: string, kolor: string}`

- [ ] **Krok 1: Napisz testy**

Plik `tests/konta.test.js`:

```javascript
import { describe, test, expect, beforeEach, afterEach } from 'vitest'
import { mkdtemp, rm, readFile, writeFile } from 'node:fs/promises'
import { tmpdir } from 'node:os'
import path from 'node:path'
import {
  wczytajKonta,
  zapiszKonta,
  waliduj,
  utworzIdKonta,
  WERSJA_SCHEMATU,
} from '../src/glowny/konta.js'

let katalog
let plik

beforeEach(async () => {
  katalog = await mkdtemp(path.join(tmpdir(), 'msghub-'))
  plik = path.join(katalog, 'accounts.json')
})

afterEach(async () => {
  await rm(katalog, { recursive: true, force: true })
})

describe('wczytajKonta', () => {
  test('brak pliku daje pusta liste, nie blad', async () => {
    const wynik = await wczytajKonta(plik)
    expect(wynik.konta).toEqual([])
    expect(wynik.wersja).toBe(WERSJA_SCHEMATU)
  })

  test('czyta zapisane konta', async () => {
    const konta = [
      { id: 'acc-a', nazwa: 'A', platforma: 'whatsapp', url: 'https://web.whatsapp.com/', kolor: '#2f7d5b' },
    ]
    await zapiszKonta(plik, konta)
    const wynik = await wczytajKonta(plik)
    expect(wynik.konta).toHaveLength(1)
    expect(wynik.konta[0].id).toBe('acc-a')
  })

  test('uszkodzony plik odklada kopie zapasowa i startuje pusto', async () => {
    await writeFile(plik, '{to nie jest json', 'utf8')
    const wynik = await wczytajKonta(plik)
    expect(wynik.konta).toEqual([])
    const kopia = await readFile(plik + '.uszkodzony', 'utf8')
    expect(kopia).toContain('to nie jest json')
  })
})

describe('waliduj', () => {
  const poprawne = {
    id: 'acc-a',
    nazwa: 'A',
    platforma: 'whatsapp',
    url: 'https://web.whatsapp.com/',
    kolor: '#2f7d5b',
  }

  test('poprawne konto nie ma bledow', () => {
    expect(waliduj(poprawne)).toEqual([])
  })

  test('pusta nazwa jest bledem', () => {
    expect(waliduj({ ...poprawne, nazwa: '  ' })).toContain('nazwa jest wymagana')
  })

  test('nieznana platforma jest bledem', () => {
    expect(waliduj({ ...poprawne, platforma: 'signal' })).toContain('nieznana platforma: signal')
  })

  test('adres inny niz https jest bledem', () => {
    expect(waliduj({ ...poprawne, url: 'http://web.whatsapp.com/' })).toContain(
      'adres musi zaczynac sie od https://',
    )
  })
})

describe('utworzIdKonta', () => {
  test('tworzy id ze znakow bezpiecznych', () => {
    expect(utworzIdKonta('WhatsApp sluzbowy', [])).toMatch(/^acc-[a-z0-9-]+$/)
  })

  test('unika kolizji z istniejacym id', () => {
    const pierwsze = utworzIdKonta('WhatsApp', [])
    const drugie = utworzIdKonta('WhatsApp', [pierwsze])
    expect(drugie).not.toBe(pierwsze)
  })

  test('polskie znaki sa transliterowane, nie wycinane do pustki', () => {
    expect(utworzIdKonta('S\u0142u\u017cbowy', [])).toBe('acc-sluzbowy')
  })
})

describe('zapiszKonta', () => {
  test('odmawia zapisu przy duplikacie id', async () => {
    const konta = [
      { id: 'acc-a', nazwa: 'A', platforma: 'whatsapp', url: 'https://web.whatsapp.com/', kolor: '#2f7d5b' },
      { id: 'acc-a', nazwa: 'B', platforma: 'whatsapp', url: 'https://web.whatsapp.com/', kolor: '#2f7d5b' },
    ]
    await expect(zapiszKonta(plik, konta)).rejects.toThrow(/duplikat id/)
  })
})
```

- [ ] **Krok 2: Uruchom testy — muszą zawieść**

```bash
npm test
```

Oczekiwane: FAIL — brak modułu `konta.js`.

- [ ] **Krok 3: Napisz `src/glowny/konta.js`**

```javascript
import { readFile, writeFile, rename } from 'node:fs/promises'

export const WERSJA_SCHEMATU = 1

export const PLATFORMY = {
  whatsapp: { url: 'https://web.whatsapp.com/', nazwaDomyslna: 'WhatsApp' },
  messenger: { url: 'https://www.messenger.com/', nazwaDomyslna: 'Messenger' },
}

export function utworzIdKonta(nazwa, istniejaceId = []) {
  const rdzen = String(nazwa)
    .toLowerCase()
    .normalize('NFD')
    .replace(/[\u0300-\u036f]/g, '')
    .replace(/\u0142/g, 'l')
    .replace(/[^a-z0-9]+/g, '-')
    .replace(/^-+|-+$/g, '')
    .slice(0, 30)
  const podstawa = `acc-${rdzen || 'konto'}`
  if (!istniejaceId.includes(podstawa)) return podstawa
  let numer = 2
  while (istniejaceId.includes(`${podstawa}-${numer}`)) numer += 1
  return `${podstawa}-${numer}`
}

export function waliduj(konto) {
  const bledy = []
  if (!konto || typeof konto !== 'object') return ['konto musi byc obiektem']
  if (!konto.id || !/^acc-[a-z0-9-]+$/.test(konto.id)) bledy.push('id musi pasowac do acc-[a-z0-9-]+')
  if (!konto.nazwa || !String(konto.nazwa).trim()) bledy.push('nazwa jest wymagana')
  if (!PLATFORMY[konto.platforma]) bledy.push(`nieznana platforma: ${konto.platforma}`)
  if (!String(konto.url || '').startsWith('https://')) bledy.push('adres musi zaczynac sie od https://')
  if (!/^#[0-9a-fA-F]{6}$/.test(konto.kolor || '')) bledy.push('kolor musi byc w formacie #rrggbb')
  return bledy
}

export async function wczytajKonta(sciezkaPliku) {
  let surowe
  try {
    surowe = await readFile(sciezkaPliku, 'utf8')
  } catch (blad) {
    if (blad.code === 'ENOENT') return { wersja: WERSJA_SCHEMATU, konta: [] }
    throw blad
  }
  try {
    const dane = JSON.parse(surowe)
    const konta = Array.isArray(dane.konta) ? dane.konta.filter((k) => waliduj(k).length === 0) : []
    return { wersja: dane.wersja ?? WERSJA_SCHEMATU, konta }
  } catch {
    await rename(sciezkaPliku, sciezkaPliku + '.uszkodzony')
    return { wersja: WERSJA_SCHEMATU, konta: [] }
  }
}

export async function zapiszKonta(sciezkaPliku, konta) {
  const widziane = new Set()
  for (const konto of konta) {
    const bledy = waliduj(konto)
    if (bledy.length) throw new Error(`niepoprawne konto ${konto?.id}: ${bledy.join(', ')}`)
    if (widziane.has(konto.id)) throw new Error(`duplikat id: ${konto.id}`)
    widziane.add(konto.id)
  }
  const tresc = JSON.stringify({ wersja: WERSJA_SCHEMATU, konta }, null, 2)
  await writeFile(sciezkaPliku + '.tmp', tresc, 'utf8')
  await rename(sciezkaPliku + '.tmp', sciezkaPliku)
}
```

- [ ] **Krok 4: Uruchom testy — muszą przejść**

```bash
npm test
```

Oczekiwane: PASS, 11 testów.

- [ ] **Krok 5: Commit**

```bash
git add src/glowny/konta.js tests/konta.test.js
git commit -m "feat: rejestr kont z walidacja i odzyskiwaniem po uszkodzonym pliku"
```

---

### Zadanie A3: Widoki kont z izolowanymi sesjami

To sedno produktu. Test izolacji jest tu ważniejszy niż sama implementacja.

**Pliki:**
- Utwórz: `src/glowny/widoki.js`
- Modyfikuj: `src/glowny/main.js`
- Test: `tests/widoki.test.js`, `tests/e2e/izolacja.spec.js`

**Interfejsy:**
- Konsumuje: `Konto` z `konta.js`
- Produkuje:
  - `czystyUserAgent(domyslnyUA)` → `string`
  - `licznikZTytulu(tytul)` → `number`
  - `utworzWidok(konto, domyslnyUA, przyBledzie)` → `WebContentsView`; `przyBledzie` to `({konto, kod, opis, adres}) => void`
  - `class ZarzadcaWidokow(okno, domyslnyUA, przyBledzie)` z metodami: `dodaj(konto)`, `pokaz(idKonta)`, `dopasujGeometrie(prostokat)`, `aktywny()`, `wszystkie()`, `sumaNieprzeczytanych()`

- [ ] **Krok 1: Napisz testy jednostkowe**

Plik `tests/widoki.test.js`:

```javascript
import { describe, test, expect } from 'vitest'
import { licznikZTytulu, czystyUserAgent } from '../src/glowny/widoki.js'

describe('czystyUserAgent', () => {
  test('usuwa znacznik Electrona', () => {
    const ua = czystyUserAgent('Mozilla/5.0 Chrome/141.0.0.0 Electron/43.4.1 Safari/537.36')
    expect(ua).not.toMatch(/Electron/)
    expect(ua).toContain('Chrome/141.0.0.0')
  })

  test('usuwa nazwe aplikacji', () => {
    expect(czystyUserAgent('Mozilla/5.0 msg-hub/0.1.0 Chrome/141.0.0.0')).not.toMatch(/msg-hub/)
  })
})

describe('licznikZTytulu', () => {
  test('czyta licznik z nawiasu', () => {
    expect(licznikZTytulu('(3) WhatsApp')).toBe(3)
  })

  test('czyta licznik wielocyfrowy', () => {
    expect(licznikZTytulu('(12) Messenger')).toBe(12)
  })

  test('brak licznika daje zero', () => {
    expect(licznikZTytulu('WhatsApp')).toBe(0)
  })

  test('pusty tytul daje zero', () => {
    expect(licznikZTytulu('')).toBe(0)
  })
})
```

- [ ] **Krok 2: Napisz test izolacji**

Plik `tests/e2e/izolacja.spec.js`:

```javascript
import { test, expect, _electron as electron } from '@playwright/test'

test('ciasteczko z jednej partycji jest niewidoczne w drugiej', async () => {
  const aplikacja = await electron.launch({ args: ['.'] })

  const wynik = await aplikacja.evaluate(async ({ session }) => {
    const pierwsza = session.fromPartition('persist:test-izolacja-a')
    const druga = session.fromPartition('persist:test-izolacja-b')

    await pierwsza.cookies.set({ url: 'https://przyklad.test', name: 'probka', value: 'wartosc-a' })

    const wPierwszej = await pierwsza.cookies.get({ name: 'probka' })
    const wDrugiej = await druga.cookies.get({ name: 'probka' })

    return { pierwsza: wPierwszej.length, druga: wDrugiej.length }
  })

  expect(wynik.pierwsza).toBe(1)
  expect(wynik.druga).toBe(0)

  await aplikacja.close()
})

test('User-Agent nie zdradza Electrona', async () => {
  const aplikacja = await electron.launch({ args: ['.'] })
  const ua = await aplikacja.evaluate(async ({ app }) => app.userAgentFallback)

  expect(ua).not.toMatch(/Electron/i)
  expect(ua).not.toMatch(/msg-hub/i)
  expect(ua).toMatch(/Chrome\/\d+/)

  await aplikacja.close()
})
```

- [ ] **Krok 3: Uruchom testy — muszą zawieść**

```bash
npm test && npm run test:e2e
```

Oczekiwane: `npm test` FAIL (brak `widoki.js`); test `User-Agent` FAIL (domyślny UA zawiera `Electron/43.4.1`).

- [ ] **Krok 4: Napisz `src/glowny/widoki.js`**

```javascript
import { WebContentsView } from 'electron'

export function czystyUserAgent(domyslnyUA) {
  return String(domyslnyUA)
    .replace(/\s*Electron\/[^\s]+/gi, '')
    .replace(/\s*msg-hub\/[^\s]+/gi, '')
    .replace(/\s{2,}/g, ' ')
    .trim()
}

export function licznikZTytulu(tytul) {
  const trafienie = /^\((\d+)\)/.exec(String(tytul || '').trim())
  return trafienie ? Number(trafienie[1]) : 0
}

export function utworzWidok(konto, domyslnyUA, przyBledzie = () => {}) {
  const widok = new WebContentsView({
    webPreferences: {
      partition: `persist:${konto.id}`,
      contextIsolation: true,
      nodeIntegration: false,
      sandbox: true,
    },
  })
  widok.webContents.setUserAgent(czystyUserAgent(domyslnyUA))

  // Spec sekcja 8: nieudane ladowanie ma dac jawny komunikat, nie puste okno.
  // Odrzucenia po User-Agent nie da sie wykryc programowo bez czytania DOM strony,
  // a to lamalaby regule 7.2 — dlatego komunikat wskazuje obie mozliwe przyczyny.
  widok.webContents.on('did-fail-load', (_zdarzenie, kod, opis, adres, glowneOkno) => {
    if (!glowneOkno || kod === -3) return // -3 = przerwane przez uzytkownika
    przyBledzie({ konto, kod, opis, adres })
  })

  widok.webContents.loadURL(konto.url)
  return widok
}

export class ZarzadcaWidokow {
  constructor(okno, domyslnyUA, przyBledzie = () => {}) {
    this.okno = okno
    this.domyslnyUA = domyslnyUA
    this.przyBledzie = przyBledzie
    this.widoki = new Map()
    this.idAktywnego = null
    this.geometria = { x: 0, y: 0, width: 0, height: 0 }
  }

  dodaj(konto) {
    if (this.widoki.has(konto.id)) return this.widoki.get(konto.id)
    const widok = utworzWidok(konto, this.domyslnyUA, this.przyBledzie)
    this.okno.contentView.addChildView(widok)
    widok.setBounds({ ...this.geometria, height: 0 })
    this.widoki.set(konto.id, widok)
    return widok
  }

  pokaz(idKonta) {
    if (!this.widoki.has(idKonta)) return
    this.idAktywnego = idKonta
    for (const [id, widok] of this.widoki) {
      widok.setBounds(id === idKonta ? this.geometria : { ...this.geometria, height: 0 })
    }
    this.widoki.get(idKonta).webContents.focus()
  }

  dopasujGeometrie(prostokat) {
    this.geometria = prostokat
    if (this.idAktywnego) this.pokaz(this.idAktywnego)
  }

  aktywny() {
    return this.idAktywnego ? this.widoki.get(this.idAktywnego) ?? null : null
  }

  wszystkie() {
    return this.widoki
  }

  sumaNieprzeczytanych() {
    let suma = 0
    for (const widok of this.widoki.values()) {
      suma += licznikZTytulu(widok.webContents.getTitle())
    }
    return suma
  }
}
```

- [ ] **Krok 5: Podłącz `User-Agent` w `src/glowny/main.js`**

Zamień całą zawartość pliku na:

```javascript
import { app, BrowserWindow } from 'electron'
import { czystyUserAgent } from './widoki.js'

app.userAgentFallback = czystyUserAgent(app.userAgentFallback)

function utworzOkno() {
  const okno = new BrowserWindow({
    width: 1280,
    height: 800,
    title: 'msg-hub',
    backgroundColor: '#111b21',
  })
  okno.setTitle('msg-hub')
  return okno
}

app.whenReady().then(() => {
  utworzOkno()
})

app.on('window-all-closed', () => {
  app.quit()
})
```

- [ ] **Krok 6: Uruchom testy — muszą przejść**

```bash
npm test && npm run test:e2e
```

Oczekiwane: PASS — 17 testów jednostkowych, 3 e2e.

- [ ] **Krok 7: Commit**

```bash
git add src/glowny/widoki.js src/glowny/main.js tests/widoki.test.js tests/e2e/izolacja.spec.js
git commit -m "feat: izolowane widoki kont z czystym User-Agent"
```

---

### Zadanie A4: Trwały układ okna

**Pliki:**
- Utwórz: `src/glowny/powloka.js`
- Test: `tests/powloka.test.js`

**Interfejsy:**
- Konsumuje: nic
- Produkuje:
  - `UKLAD_DOMYSLNY` → `{szerokosc: 1280, wysokosc: 800, zmaksymalizowane: false}`
  - `async wczytajUklad(sciezkaPliku)` → `Promise<{x?: number, y?: number, szerokosc: number, wysokosc: number, zmaksymalizowane: boolean}>`
  - `async zapiszUklad(sciezkaPliku, uklad)` → `Promise<void>`
  - `ustawAutostart(wlaczony, aplikacja)` → `void`

- [ ] **Krok 1: Napisz testy**

Plik `tests/powloka.test.js`:

```javascript
import { describe, test, expect, beforeEach, afterEach } from 'vitest'
import { mkdtemp, rm, writeFile } from 'node:fs/promises'
import { tmpdir } from 'node:os'
import path from 'node:path'
import { wczytajUklad, zapiszUklad, UKLAD_DOMYSLNY } from '../src/glowny/powloka.js'

let plik, katalog

beforeEach(async () => {
  katalog = await mkdtemp(path.join(tmpdir(), 'msghub-uklad-'))
  plik = path.join(katalog, 'uklad.json')
})

afterEach(async () => {
  await rm(katalog, { recursive: true, force: true })
})

describe('uklad okna', () => {
  test('brak pliku daje uklad domyslny', async () => {
    expect(await wczytajUklad(plik)).toEqual(UKLAD_DOMYSLNY)
  })

  test('uklad przezywa zapis i odczyt', async () => {
    await zapiszUklad(plik, { x: 100, y: 50, szerokosc: 1000, wysokosc: 700, zmaksymalizowane: false })
    const wynik = await wczytajUklad(plik)
    expect(wynik.szerokosc).toBe(1000)
    expect(wynik.x).toBe(100)
  })

  test('uszkodzony plik daje uklad domyslny zamiast wyjatku', async () => {
    await writeFile(plik, 'nie-json', 'utf8')
    expect(await wczytajUklad(plik)).toEqual(UKLAD_DOMYSLNY)
  })

  test('absurdalne rozmiary sa przycinane do minimum', async () => {
    await zapiszUklad(plik, { szerokosc: 10, wysokosc: 10, zmaksymalizowane: false })
    const wynik = await wczytajUklad(plik)
    expect(wynik.szerokosc).toBeGreaterThanOrEqual(800)
    expect(wynik.wysokosc).toBeGreaterThanOrEqual(600)
  })
})
```

- [ ] **Krok 2: Uruchom testy — muszą zawieść**

```bash
npm test
```

- [ ] **Krok 3: Napisz `src/glowny/powloka.js`**

```javascript
import { readFile, writeFile, rename } from 'node:fs/promises'

export const UKLAD_DOMYSLNY = { szerokosc: 1280, wysokosc: 800, zmaksymalizowane: false }

const MIN_SZEROKOSC = 800
const MIN_WYSOKOSC = 600

export async function wczytajUklad(sciezkaPliku) {
  try {
    const dane = JSON.parse(await readFile(sciezkaPliku, 'utf8'))
    return {
      ...dane,
      szerokosc: Math.max(MIN_SZEROKOSC, Number(dane.szerokosc) || UKLAD_DOMYSLNY.szerokosc),
      wysokosc: Math.max(MIN_WYSOKOSC, Number(dane.wysokosc) || UKLAD_DOMYSLNY.wysokosc),
      zmaksymalizowane: Boolean(dane.zmaksymalizowane),
    }
  } catch {
    return { ...UKLAD_DOMYSLNY }
  }
}

export async function zapiszUklad(sciezkaPliku, uklad) {
  await writeFile(sciezkaPliku + '.tmp', JSON.stringify(uklad, null, 2), 'utf8')
  await rename(sciezkaPliku + '.tmp', sciezkaPliku)
}

export function ustawAutostart(wlaczony, aplikacja) {
  aplikacja.setLoginItemSettings({ openAtLogin: Boolean(wlaczony), openAsHidden: true })
}
```

- [ ] **Krok 4: Uruchom testy — muszą przejść**

```bash
npm test
```

Oczekiwane: PASS, 21 testów.

- [ ] **Krok 5: Commit**

```bash
git add src/glowny/powloka.js tests/powloka.test.js
git commit -m "feat: trwaly uklad okna z przycinaniem absurdalnych rozmiarow"
```

---

### Zadanie A5: Interfejs zakładek i dodawanie kont

Po tym zadaniu aplikacja jest po raz pierwszy używalna ręcznie.

**Pliki:**
- Utwórz: `src/glowny/most.js`, `src/preload/preload.js`, `src/renderer/index.html`, `src/renderer/renderer.js`, `src/renderer/styl.css`
- Modyfikuj: `src/glowny/main.js`

**Interfejsy:**
- Konsumuje: `konta.js`, `widoki.js`, `powloka.js`
- Produkuje: `window.mostHub` w rendererze z metodami:
  - `listaKont()` → `Promise<Konto[]>`
  - `dodajKonto({nazwa, platforma, kolor})` → `Promise<{ok: boolean, bledy?: string[]}>`
  - `przelacz(idKonta)` → `Promise<void>`
- Produkuje też: `zarejestrujKanalyKont({katalogDanych, zarzadca, poDodaniuKonta})` w `most.js`

- [ ] **Krok 1: Napisz `src/preload/preload.js`**

```javascript
import { contextBridge, ipcRenderer } from 'electron'

contextBridge.exposeInMainWorld('mostHub', {
  listaKont: () => ipcRenderer.invoke('konta:lista'),
  dodajKonto: (dane) => ipcRenderer.invoke('konta:dodaj', dane),
  przelacz: (idKonta) => ipcRenderer.invoke('konta:przelacz', idKonta),
})
```

- [ ] **Krok 2: Napisz `src/glowny/most.js`**

```javascript
import { ipcMain } from 'electron'
import path from 'node:path'
import { wczytajKonta, zapiszKonta, waliduj, utworzIdKonta, PLATFORMY } from './konta.js'

export function zarejestrujKanalyKont({ katalogDanych, zarzadca, poDodaniuKonta }) {
  const plikKont = path.join(katalogDanych, 'accounts.json')

  ipcMain.handle('konta:lista', async () => (await wczytajKonta(plikKont)).konta)

  ipcMain.handle('konta:dodaj', async (_zdarzenie, dane) => {
    const { konta } = await wczytajKonta(plikKont)
    const platforma = PLATFORMY[dane.platforma]
    const konto = {
      id: utworzIdKonta(dane.nazwa, konta.map((k) => k.id)),
      nazwa: String(dane.nazwa || '').trim(),
      platforma: dane.platforma,
      url: dane.url || platforma?.url || '',
      kolor: dane.kolor || '#2f7d5b',
    }
    const bledy = waliduj(konto)
    if (bledy.length) return { ok: false, bledy }
    await zapiszKonta(plikKont, [...konta, konto])
    zarzadca.dodaj(konto)
    poDodaniuKonta()
    return { ok: true }
  })

  ipcMain.handle('konta:przelacz', async (_zdarzenie, idKonta) => {
    zarzadca.pokaz(idKonta)
  })
}
```

- [ ] **Krok 3: Napisz `src/renderer/index.html`**

```html
<!doctype html>
<meta charset="utf-8" />
<title>msg-hub</title>
<link rel="stylesheet" href="styl.css" />
<header id="pasek">
  <nav id="zakladki"></nav>
  <button id="dodaj-konto" title="Dodaj konto">+</button>
</header>
<dialog id="okno-konta">
  <form method="dialog">
    <h2>Dodaj konto</h2>
    <label>Nazwa <input name="nazwa" required /></label>
    <label>Platforma
      <select name="platforma">
        <option value="whatsapp">WhatsApp</option>
        <option value="messenger">Messenger</option>
      </select>
    </label>
    <label>Kolor zakladki <input name="kolor" type="color" value="#2f7d5b" /></label>
    <p id="bledy-konta" class="bledy"></p>
    <menu>
      <button value="anuluj">Anuluj</button>
      <button id="zapisz-konto" value="zapisz">Zapisz</button>
    </menu>
  </form>
</dialog>
<script type="module" src="renderer.js"></script>
```

- [ ] **Krok 4: Napisz `src/renderer/styl.css`**

```css
:root { color-scheme: dark; --tlo: #111b21; --panel: #202c33; --tekst: #e9edef; --akcent: #2f7d5b; }
* { box-sizing: border-box; }
body { margin: 0; font: 14px/1.4 "Segoe UI", system-ui, sans-serif; background: var(--tlo); color: var(--tekst); }
#pasek { display: flex; align-items: center; gap: 4px; height: 44px; padding: 0 8px; background: var(--panel); }
#zakladki { display: flex; gap: 4px; flex: 1; }
.zakladka { padding: 6px 14px; border: 0; border-radius: 6px; background: transparent; color: var(--tekst); cursor: pointer; border-bottom: 3px solid transparent; }
.zakladka[aria-selected="true"] { background: #2a3942; border-bottom-color: var(--kolor-konta, var(--akcent)); }
button { border: 0; border-radius: 6px; padding: 6px 12px; background: #2a3942; color: var(--tekst); cursor: pointer; }
dialog { border: 0; border-radius: 10px; background: var(--panel); color: var(--tekst); min-width: 420px; }
dialog label { display: block; margin: 10px 0; }
dialog input, dialog select, dialog textarea { width: 100%; padding: 6px; border-radius: 6px; border: 1px solid #3b4a54; background: var(--tlo); color: var(--tekst); font: inherit; }
.bledy { color: #f15c6d; min-height: 1em; }
```

- [ ] **Krok 5: Napisz `src/renderer/renderer.js`**

```javascript
const zakladki = document.getElementById('zakladki')
const oknoKonta = document.getElementById('okno-konta')
const bledyKonta = document.getElementById('bledy-konta')

async function odswiezZakladki() {
  const konta = await window.mostHub.listaKont()
  zakladki.replaceChildren()
  konta.forEach((konto, indeks) => {
    const przycisk = document.createElement('button')
    przycisk.className = 'zakladka'
    przycisk.textContent = konto.nazwa
    przycisk.style.setProperty('--kolor-konta', konto.kolor)
    przycisk.setAttribute('aria-selected', String(indeks === 0))
    przycisk.addEventListener('click', async () => {
      await window.mostHub.przelacz(konto.id)
      for (const inny of zakladki.children) inny.setAttribute('aria-selected', 'false')
      przycisk.setAttribute('aria-selected', 'true')
    })
    zakladki.append(przycisk)
  })
  if (konta.length) await window.mostHub.przelacz(konta[0].id)
}

document.getElementById('dodaj-konto').addEventListener('click', () => {
  bledyKonta.textContent = ''
  oknoKonta.showModal()
})

document.getElementById('zapisz-konto').addEventListener('click', async (zdarzenie) => {
  zdarzenie.preventDefault()
  const dane = Object.fromEntries(new FormData(oknoKonta.querySelector('form')))
  const wynik = await window.mostHub.dodajKonto(dane)
  if (!wynik.ok) {
    bledyKonta.textContent = wynik.bledy.join('; ')
    return
  }
  oknoKonta.close()
  await odswiezZakladki()
})

odswiezZakladki()
```

- [ ] **Krok 6: Złóż całość w `src/glowny/main.js`**

Zamień całą zawartość pliku na:

```javascript
import { app, BrowserWindow, Tray, Menu, nativeImage, dialog } from 'electron'
import path from 'node:path'
import { fileURLToPath } from 'node:url'
import { czystyUserAgent, ZarzadcaWidokow } from './widoki.js'
import { wczytajUklad, zapiszUklad } from './powloka.js'
import { wczytajKonta } from './konta.js'
import { zarejestrujKanalyKont } from './most.js'

const KATALOG = path.dirname(fileURLToPath(import.meta.url))
const WYSOKOSC_PASKA = 44

app.userAgentFallback = czystyUserAgent(app.userAgentFallback)

let okno
let zasobnik
let zarzadca

async function utworzOkno() {
  const katalogDanych = app.getPath('userData')
  const plikUkladu = path.join(katalogDanych, 'uklad.json')
  const uklad = await wczytajUklad(plikUkladu)

  okno = new BrowserWindow({
    width: uklad.szerokosc,
    height: uklad.wysokosc,
    x: uklad.x,
    y: uklad.y,
    minWidth: 800,
    minHeight: 600,
    title: 'msg-hub',
    backgroundColor: '#111b21',
    webPreferences: { preload: path.join(KATALOG, '..', 'preload', 'preload.js') },
  })
  okno.setTitle('msg-hub')
  if (uklad.zmaksymalizowane) okno.maximize()
  await okno.loadFile(path.join(KATALOG, '..', 'renderer', 'index.html'))

  zarzadca = new ZarzadcaWidokow(okno, app.userAgentFallback, ({ konto, kod, opis }) => {
    dialog.showErrorBox(
      `Nie udalo sie zaladowac konta ${konto.nazwa}`,
      `Blad ${kod}: ${opis}\n\n` +
        'Jesli siec dziala, a strona odmawia obslugi klienta, zaktualizuj aplikacje ' +
        '(npm install electron@latest) — WhatsApp Web wymaga swiezej wersji Chromium.',
    )
  })

  const dopasuj = () => {
    const { width, height } = okno.getContentBounds()
    zarzadca.dopasujGeometrie({ x: 0, y: WYSOKOSC_PASKA, width, height: height - WYSOKOSC_PASKA })
  }
  okno.on('resize', dopasuj)

  const { konta } = await wczytajKonta(path.join(katalogDanych, 'accounts.json'))
  for (const konto of konta) zarzadca.dodaj(konto)
  dopasuj()
  if (konta.length) zarzadca.pokaz(konta[0].id)

  zarejestrujKanalyKont({ katalogDanych, zarzadca, poDodaniuKonta: dopasuj })

  okno.on('close', async () => {
    const prostokat = okno.getNormalBounds()
    await zapiszUklad(plikUkladu, {
      x: prostokat.x,
      y: prostokat.y,
      szerokosc: prostokat.width,
      wysokosc: prostokat.height,
      zmaksymalizowane: okno.isMaximized(),
    })
  })
}

function utworzZasobnik() {
  zasobnik = new Tray(nativeImage.createEmpty())
  zasobnik.setToolTip('msg-hub')
  zasobnik.setContextMenu(
    Menu.buildFromTemplate([
      { label: 'Pokaz', click: () => okno?.show() },
      { type: 'separator' },
      { label: 'Zakoncz', click: () => app.quit() },
    ]),
  )
  zasobnik.on('click', () => (okno?.isVisible() ? okno.hide() : okno?.show()))
}

app.whenReady().then(async () => {
  await utworzOkno()
  utworzZasobnik()
})

app.on('window-all-closed', () => app.quit())
```

- [ ] **Krok 7: Uruchom aplikację i przejdź ścieżkę użytkownika**

```bash
npm start
```

Sprawdź: okno wstaje z ciemnym paskiem; `+` otwiera formularz; po dodaniu konta WhatsApp pojawia się zakładka i ładuje kod QR.

- [ ] **Krok 8: Uruchom pełne testy**

```bash
npm test && npm run test:e2e
```

- [ ] **Krok 9: Commit**

```bash
git add src/glowny/most.js src/preload src/renderer src/glowny/main.js
git commit -m "feat: interfejs zakladek, dodawanie kont, zasobnik systemowy"
```

---

### Zadanie A6: Powiadomienia systemowe i licznik nieprzeczytanych

**Pliki:**
- Modyfikuj: `src/glowny/main.js`

**Interfejsy:**
- Konsumuje: `ZarzadcaWidokow.sumaNieprzeczytanych()` z zadania A3
- Produkuje: powiadomienia Windows dla wszystkich kont oraz badge na ikonie paska zadań

- [ ] **Krok 1: W `src/glowny/main.js` zastąp pętlę dodającą konta**

Znajdź fragment:

```javascript
  const { konta } = await wczytajKonta(path.join(katalogDanych, 'accounts.json'))
  for (const konto of konta) zarzadca.dodaj(konto)
  dopasuj()
  if (konta.length) zarzadca.pokaz(konta[0].id)
```

Zamień na:

```javascript
  const { konta } = await wczytajKonta(path.join(katalogDanych, 'accounts.json'))
  for (const konto of konta) {
    const widok = zarzadca.dodaj(konto)
    widok.webContents.session.setPermissionRequestHandler((_wc, uprawnienie, przyznaj) => {
      przyznaj(uprawnienie === 'notifications')
    })
    widok.webContents.on('page-title-updated', odswiezBadge)
  }
  dopasuj()
  if (konta.length) zarzadca.pokaz(konta[0].id)
```

- [ ] **Krok 2: Dodaj funkcję `odswiezBadge` powyżej tej pętli**

Wstaw tuż po definicji `dopasuj`:

```javascript
  const odswiezBadge = () => {
    const suma = zarzadca.sumaNieprzeczytanych()
    app.setBadgeCount(suma)
    okno.setOverlayIcon(null, suma ? `${suma} nieprzeczytanych` : '')
  }
```

- [ ] **Krok 3: Podłącz to samo dla kont dodawanych w trakcie działania**

W `src/glowny/most.js`, w handlerze `konta:dodaj`, zamień `zarzadca.dodaj(konto)` na:

```javascript
    const widok = zarzadca.dodaj(konto)
    widok.webContents.session.setPermissionRequestHandler((_wc, uprawnienie, przyznaj) => {
      przyznaj(uprawnienie === 'notifications')
    })
```

- [ ] **Krok 4: Uruchom aplikację i sprawdź powiadomienie**

```bash
npm start
```

Wyślij sobie wiadomość z telefonu. Oczekiwane: powiadomienie Windows w rogu ekranu, licznik na ikonie paska zadań.

- [ ] **Krok 5: Commit**

```bash
git add src/glowny/main.js src/glowny/most.js
git commit -m "feat: powiadomienia systemowe i licznik nieprzeczytanych"
```

---

## PRZEGLĄD PO FAZIE A

Zatrzymaj się. Nie zaczynaj fazy B przed rozmową z operatorem.

**Wykonawca uruchamia i raportuje wynik:**

```bash
npm test && npm run test:e2e && git log --oneline && git status --short
```

**Operator sprawdza ręcznie:**

1. `npm start` — okno wstaje, pasek jest ciemny
2. dodanie trzech kont: Messenger, WhatsApp prywatny, WhatsApp służbowy
3. **każde konto loguje się osobno** — to dowód izolacji w praktyce, nie tylko w teście
4. przełączanie zakładek działa, strony nie przeładowują się przy zmianie
5. zamknięcie i ponowne otwarcie — konta nadal zalogowane, okno w tym samym miejscu
6. wiadomość z telefonu daje powiadomienie Windows

**Kryterium przejścia dalej:** punkt 3 i 5 muszą działać. Bez nich produkt nie ma sensu i nie ma po co budować makr.

---

# FAZA B — Makra tekstowe

Cel fazy: `Ctrl+;` otwiera panel makr, wybór makra wstawia sformatowany tekst do pola wiadomości, edytor pozwala zapisać nowe makro z podglądem formatowania.

---

### Zadanie B1: Magazyn makr

**Pliki:**
- Utwórz: `src/glowny/makra.js`
- Test: `tests/makra.test.js`

**Interfejsy:**
- Konsumuje: nic
- Produkuje:
  - `WERSJA_SCHEMATU_MAKR` → `number` (1)
  - `utworzIdMakra(nazwa)` → `string`
  - `async wczytajMakra(sciezkaPliku)` → `Promise<{wersja: number, makra: Makro[]}>`
  - `async zapiszMakra(sciezkaPliku, makra)` → `Promise<void>`
  - `szukaj(makra, fraza)` → `Makro[]`
  - `Makro` = `{id: string, nazwa: string, tekst: string, zalaczniki: string[], tagi: string[]}`

- [ ] **Krok 1: Napisz testy**

Plik `tests/makra.test.js`:

```javascript
import { describe, test, expect, beforeEach, afterEach } from 'vitest'
import { mkdtemp, rm } from 'node:fs/promises'
import { tmpdir } from 'node:os'
import path from 'node:path'
import { wczytajMakra, zapiszMakra, szukaj, utworzIdMakra } from '../src/glowny/makra.js'

let plik, katalog

const makro = (nadpisania = {}) => ({
  id: 'mac-test',
  nazwa: 'Instrukcja Strefa Klienta',
  tekst: '*Jak dodac kierowce:*\n1. Zaloguj sie',
  zalaczniki: [],
  tagi: ['strefa'],
  ...nadpisania,
})

beforeEach(async () => {
  katalog = await mkdtemp(path.join(tmpdir(), 'msghub-makra-'))
  plik = path.join(katalog, 'macros.json')
})

afterEach(async () => {
  await rm(katalog, { recursive: true, force: true })
})

describe('magazyn makr', () => {
  test('brak pliku daje pusta liste', async () => {
    expect((await wczytajMakra(plik)).makra).toEqual([])
  })

  test('makro przezywa zapis i odczyt razem z formatowaniem', async () => {
    await zapiszMakra(plik, [makro()])
    const wynik = await wczytajMakra(plik)
    expect(wynik.makra[0].tekst).toBe('*Jak dodac kierowce:*\n1. Zaloguj sie')
  })
})

describe('szukaj', () => {
  const zbior = [
    makro({ id: 'mac-a', nazwa: 'Strefa Klienta', tekst: 'logowanie', tagi: ['strefa'] }),
    makro({ id: 'mac-b', nazwa: 'Passango', tekst: 'instalacja urzadzenia', tagi: ['passango'] }),
  ]

  test('znajduje po nazwie bez wzgledu na wielkosc liter', () => {
    expect(szukaj(zbior, 'strefa').map((m) => m.id)).toEqual(['mac-a'])
  })

  test('znajduje po tresci', () => {
    expect(szukaj(zbior, 'instalacja').map((m) => m.id)).toEqual(['mac-b'])
  })

  test('znajduje po tagu', () => {
    expect(szukaj(zbior, 'passango').map((m) => m.id)).toEqual(['mac-b'])
  })

  test('pusta fraza zwraca wszystko', () => {
    expect(szukaj(zbior, '   ')).toHaveLength(2)
  })
})

describe('utworzIdMakra', () => {
  test('tworzy id ze znakow bezpiecznych', () => {
    expect(utworzIdMakra('Instrukcja \u2014 Strefa Klienta!')).toMatch(/^mac-[a-z0-9-]+$/)
  })

  test('polskie znaki sa transliterowane', () => {
    expect(utworzIdMakra('Za\u0142\u0105czniki')).toBe('mac-zalaczniki')
  })
})
```

- [ ] **Krok 2: Uruchom testy — muszą zawieść**

```bash
npm test
```

- [ ] **Krok 3: Napisz `src/glowny/makra.js`**

```javascript
import { readFile, writeFile, rename } from 'node:fs/promises'
import { randomUUID } from 'node:crypto'

export const WERSJA_SCHEMATU_MAKR = 1

export function utworzIdMakra(nazwa) {
  const rdzen = String(nazwa)
    .toLowerCase()
    .normalize('NFD')
    .replace(/[\u0300-\u036f]/g, '')
    .replace(/\u0142/g, 'l')
    .replace(/[^a-z0-9]+/g, '-')
    .replace(/^-+|-+$/g, '')
    .slice(0, 40)
  return `mac-${rdzen || randomUUID().slice(0, 8)}`
}

export async function wczytajMakra(sciezkaPliku) {
  try {
    const dane = JSON.parse(await readFile(sciezkaPliku, 'utf8'))
    return {
      wersja: dane.wersja ?? WERSJA_SCHEMATU_MAKR,
      makra: Array.isArray(dane.makra) ? dane.makra : [],
    }
  } catch (blad) {
    if (blad.code !== 'ENOENT') {
      await rename(sciezkaPliku, sciezkaPliku + '.uszkodzony').catch(() => {})
    }
    return { wersja: WERSJA_SCHEMATU_MAKR, makra: [] }
  }
}

export async function zapiszMakra(sciezkaPliku, makra) {
  const tresc = JSON.stringify({ wersja: WERSJA_SCHEMATU_MAKR, makra }, null, 2)
  await writeFile(sciezkaPliku + '.tmp', tresc, 'utf8')
  await rename(sciezkaPliku + '.tmp', sciezkaPliku)
}

export function szukaj(makra, fraza) {
  const igla = String(fraza || '').trim().toLowerCase()
  if (!igla) return [...makra]
  return makra.filter((m) => {
    const stog = [m.nazwa, m.tekst, ...(m.tagi ?? [])].join(' ').toLowerCase()
    return stog.includes(igla)
  })
}
```

- [ ] **Krok 4: Uruchom testy — muszą przejść**

```bash
npm test
```

Oczekiwane: PASS, 29 testów.

- [ ] **Krok 5: Commit**

```bash
git add src/glowny/makra.js tests/makra.test.js
git commit -m "feat: magazyn makr z wyszukiwaniem po nazwie, tresci i tagach"
```

---

### Zadanie B2: Wstawianie treści i egzekucja granic

Tu żyją reguły 7.1 i 7.2 specu. Testy negatywne są ważniejsze niż funkcja.

**Pliki:**
- Utwórz: `src/glowny/wstawianie.js`, `tests/granice.test.js`
- Test: `tests/wstawianie.test.js`

**Interfejsy:**
- Konsumuje: nic
- Produkuje: `wstawTekst(webContents, tekst, schowek)` → `void`

`schowek` jest parametrem, nie importem — dzięki temu moduł testuje się bez Electrona.

- [ ] **Krok 1: Napisz testy wstawiania**

Plik `tests/wstawianie.test.js`:

```javascript
import { describe, test, expect } from 'vitest'
import { wstawTekst } from '../src/glowny/wstawianie.js'

function atrapaWebContents() {
  const wywolania = []
  return {
    wywolania,
    paste: () => wywolania.push('paste'),
    sendInputEvent: (zdarzenie) => wywolania.push(`sendInputEvent:${zdarzenie?.keyCode ?? '?'}`),
    executeJavaScript: (kod) => wywolania.push(`executeJavaScript:${kod}`),
    focus: () => wywolania.push('focus'),
  }
}

function atrapaSchowka() {
  const zapisy = []
  return { zapisy, writeText: (t) => zapisy.push(t) }
}

describe('wstawTekst', () => {
  test('kladzie tekst w schowku i wywoluje wklejenie', () => {
    const wc = atrapaWebContents()
    const schowek = atrapaSchowka()

    wstawTekst(wc, '*Test*', schowek)

    expect(schowek.zapisy).toEqual(['*Test*'])
    expect(wc.wywolania).toContain('paste')
  })

  test('NIE wysyla wiadomosci — zadnego Entera, zadnego skryptu na stronie', () => {
    const wc = atrapaWebContents()
    wstawTekst(wc, 'dowolna tresc', atrapaSchowka())

    const zakazane = wc.wywolania.filter(
      (w) => w.startsWith('sendInputEvent') || w.startsWith('executeJavaScript'),
    )
    expect(zakazane).toEqual([])
  })

  test('pusty tekst nie rusza schowka ani widoku', () => {
    const wc = atrapaWebContents()
    const schowek = atrapaSchowka()

    wstawTekst(wc, '', schowek)

    expect(schowek.zapisy).toEqual([])
    expect(wc.wywolania).toEqual([])
  })
})
```

- [ ] **Krok 2: Uruchom testy — muszą zawieść**

```bash
npm test
```

- [ ] **Krok 3: Napisz `src/glowny/wstawianie.js`**

```javascript
// REGULA 7.1 specu: ten modul NIGDY nie wysyla wiadomosci.
// Nie wolno tu dodac sendInputEvent z Enterem ani executeJavaScript.
// Test "NIE wysyla wiadomosci" w tests/wstawianie.test.js pilnuje tej granicy.

export function wstawTekst(webContents, tekst, schowek) {
  if (!webContents || !schowek) return
  const tresc = String(tekst ?? '')
  if (!tresc) return
  schowek.writeText(tresc)
  webContents.paste()
}
```

- [ ] **Krok 4: Napisz test regresji reguły 7.2**

Plik `tests/granice.test.js`:

```javascript
import { describe, test, expect } from 'vitest'
import { readFileSync, readdirSync } from 'node:fs'
import path from 'node:path'

const ZAKAZANE_PAKIETY = [
  '@wppconnect/wa-js',
  'wppconnect',
  'whatsapp-web.js',
  '@whiskeysockets/baileys',
  'baileys',
  'venom-bot',
]

function plikiZrodlowe(katalog) {
  return readdirSync(katalog, { recursive: true, encoding: 'utf8' })
    .filter((wzgledna) => wzgledna.endsWith('.js'))
    .map((wzgledna) => path.join(katalog, wzgledna))
}

describe('regula 7.2 — zadnej ingerencji w strony Meta', () => {
  test('package.json nie zawiera bibliotek ingerujacych w WhatsApp Web', () => {
    const pakiet = JSON.parse(readFileSync('package.json', 'utf8'))
    const zaleznosci = Object.keys({ ...pakiet.dependencies, ...pakiet.devDependencies })
    for (const zakazany of ZAKAZANE_PAKIETY) {
      expect(zaleznosci).not.toContain(zakazany)
    }
  })

  test('kod zrodlowy nie wstrzykuje skryptow ani stylow do widokow kont', () => {
    const winowajcy = []
    for (const plik of plikiZrodlowe('src')) {
      const tresc = readFileSync(plik, 'utf8')
      if (/executeJavaScript|insertCSS|wppconnect|WPP\./.test(tresc)) winowajcy.push(plik)
    }
    expect(winowajcy).toEqual([])
  })
})
```

- [ ] **Krok 5: Uruchom testy — muszą przejść**

```bash
npm test
```

Oczekiwane: PASS, 34 testy. Test granic nie ma fazy czerwonej — pilnuje, żeby zakazana zależność nie wkradła się później.

- [ ] **Krok 6: Commit**

```bash
git add src/glowny/wstawianie.js tests/wstawianie.test.js tests/granice.test.js
git commit -m "feat: wstawianie tekstu przez schowek z egzekucja regul 7.1 i 7.2"
```

---

### Zadanie B3: Panel makr w interfejsie

**Pliki:**
- Modyfikuj: `src/glowny/most.js`, `src/preload/preload.js`, `src/renderer/index.html`, `src/renderer/renderer.js`, `src/renderer/styl.css`, `src/glowny/main.js`

**Interfejsy:**
- Konsumuje: `makra.js`, `wstawianie.js`
- Produkuje w `window.mostHub`:
  - `listaMakr(fraza)` → `Promise<Makro[]>`
  - `wstawMakro(idMakra)` → `Promise<{ok: boolean, brakujace: string[]}>`
- Produkuje w `most.js`: `zarejestrujKanalyMakr({katalogDanych, zarzadca})`

- [ ] **Krok 1: Dopisz kanały do `src/glowny/most.js`**

Dodaj na górze pliku:

```javascript
import { clipboard } from 'electron'
import { wczytajMakra, zapiszMakra, szukaj, utworzIdMakra } from './makra.js'
import { wstawTekst } from './wstawianie.js'
```

Dodaj na końcu pliku:

```javascript
export function zarejestrujKanalyMakr({ katalogDanych, zarzadca }) {
  const plikMakr = path.join(katalogDanych, 'macros.json')

  ipcMain.handle('makra:lista', async (_zdarzenie, fraza) => {
    const { makra } = await wczytajMakra(plikMakr)
    return szukaj(makra, fraza)
  })

  ipcMain.handle('makra:zapisz', async (_zdarzenie, makro) => {
    if (!String(makro?.nazwa || '').trim()) return { ok: false, bledy: ['nazwa jest wymagana'] }
    const { makra } = await wczytajMakra(plikMakr)
    const id = makro.id || utworzIdMakra(makro.nazwa)
    const pozostale = makra.filter((m) => m.id !== id)
    await zapiszMakra(plikMakr, [...pozostale, { zalaczniki: [], tagi: [], ...makro, id }])
    return { ok: true, id }
  })

  ipcMain.handle('makra:wstaw', async (_zdarzenie, idMakra) => {
    const { makra } = await wczytajMakra(plikMakr)
    const makro = makra.find((m) => m.id === idMakra)
    const widok = zarzadca.aktywny()
    if (!makro || !widok) return { ok: false, brakujace: [] }
    if (makro.tekst) wstawTekst(widok.webContents, makro.tekst, clipboard)
    return { ok: true, brakujace: [] }
  })
}
```

- [ ] **Krok 2: Dopisz metody do `src/preload/preload.js`**

Dodaj do obiektu przekazywanego do `exposeInMainWorld`:

```javascript
  listaMakr: (fraza) => ipcRenderer.invoke('makra:lista', fraza),
  zapiszMakro: (makro) => ipcRenderer.invoke('makra:zapisz', makro),
  wstawMakro: (idMakra) => ipcRenderer.invoke('makra:wstaw', idMakra),
```

- [ ] **Krok 3: Dopisz panel do `src/renderer/index.html`**

Dodaj przycisk w pasku, tuż po `<button id="dodaj-konto" …>`:

```html
  <button id="otworz-makra" title="Makra (Ctrl+;)">Makra</button>
```

Dodaj przed znacznikiem `<script>`:

```html
<dialog id="okno-makr">
  <input id="szukaj-makro" placeholder="Szukaj makra..." autocomplete="off" />
  <ul id="lista-makr"></ul>
  <menu>
    <button value="zamknij">Zamknij</button>
    <button id="nowe-makro" value="nowe">Nowe makro</button>
  </menu>
</dialog>
```

- [ ] **Krok 4: Dopisz style do `src/renderer/styl.css`**

```css
#lista-makr { list-style: none; margin: 8px 0; padding: 0; max-height: 50vh; overflow: auto; }
#lista-makr li { padding: 8px; border-radius: 6px; cursor: pointer; }
#lista-makr li:hover { background: #2a3942; }
#lista-makr .puste { color: #8696a0; cursor: default; }
```

- [ ] **Krok 5: Dopisz obsługę do `src/renderer/renderer.js`**

```javascript
const oknoMakr = document.getElementById('okno-makr')
const szukajka = document.getElementById('szukaj-makro')
const listaMakr = document.getElementById('lista-makr')

async function odswiezMakra() {
  const makra = await window.mostHub.listaMakr(szukajka.value)
  listaMakr.replaceChildren()
  if (!makra.length) {
    const puste = document.createElement('li')
    puste.className = 'puste'
    puste.textContent = 'Brak makr — kliknij "Nowe makro"'
    listaMakr.append(puste)
    return
  }
  for (const makro of makra) {
    const pozycja = document.createElement('li')
    const liczba = (makro.zalaczniki ?? []).length
    pozycja.textContent = liczba ? `${makro.nazwa}  (${liczba} zal.)` : makro.nazwa
    pozycja.addEventListener('click', async () => {
      oknoMakr.close()
      const wynik = await window.mostHub.wstawMakro(makro.id)
      if (wynik && !wynik.ok && wynik.brakujace.length) {
        alert(`Brakuje zalacznikow w magazynie:\n${wynik.brakujace.join('\n')}\n\nTekst zostal wstawiony.`)
      }
    })
    listaMakr.append(pozycja)
  }
}

szukajka.addEventListener('input', odswiezMakra)

document.getElementById('otworz-makra').addEventListener('click', async () => {
  szukajka.value = ''
  await odswiezMakra()
  oknoMakr.showModal()
  szukajka.focus()
})

window.addEventListener('keydown', (zdarzenie) => {
  if (zdarzenie.ctrlKey && zdarzenie.key === ';') {
    zdarzenie.preventDefault()
    document.getElementById('otworz-makra').click()
  }
})
```

- [ ] **Krok 6: Zarejestruj kanały w `src/glowny/main.js`**

Zmień import:

```javascript
import { zarejestrujKanalyKont, zarejestrujKanalyMakr } from './most.js'
```

Pod linią `zarejestrujKanalyKont({ … })` dodaj:

```javascript
  zarejestrujKanalyMakr({ katalogDanych, zarzadca })
```

- [ ] **Krok 7: Sprawdź ręcznie**

```bash
npm start
```

`Ctrl+;` otwiera panel z komunikatem o braku makr. Edytor powstaje w zadaniu B4.

- [ ] **Krok 8: Commit**

```bash
git add src/glowny/most.js src/preload/preload.js src/renderer src/glowny/main.js
git commit -m "feat: panel makr ze skrotem Ctrl+; i wstawianiem tekstu"
```

---

### Zadanie B4: Edytor makra z podglądem formatowania

**Pliki:**
- Utwórz: `src/renderer/podglad.js`
- Modyfikuj: `src/renderer/index.html`, `src/renderer/renderer.js`, `src/renderer/styl.css`
- Test: `tests/podglad.test.js`

**Interfejsy:**
- Konsumuje: `zapiszMakro` z zadania B3
- Produkuje: `naHtml(tekst)` → `string` — zamiana znaczników WhatsApp na HTML podglądu

- [ ] **Krok 1: Napisz testy podglądu**

Plik `tests/podglad.test.js`:

```javascript
import { describe, test, expect } from 'vitest'
import { naHtml } from '../src/renderer/podglad.js'

describe('naHtml', () => {
  test('pogrubienie miedzy gwiazdkami', () => {
    expect(naHtml('*wazne*')).toContain('<strong>wazne</strong>')
  })

  test('kursywa miedzy podkreslnikami', () => {
    expect(naHtml('_uwaga_')).toContain('<em>uwaga</em>')
  })

  test('przekreslenie miedzy tyldami', () => {
    expect(naHtml('~stare~')).toContain('<del>stare</del>')
  })

  test('lista punktowana z myslnika', () => {
    const html = naHtml('- pierwszy\n- drugi')
    expect(html).toContain('<li>pierwszy</li>')
    expect(html).toContain('<li>drugi</li>')
  })

  test('lista numerowana z cyfry i kropki', () => {
    expect(naHtml('1. krok')).toContain('<li>krok</li>')
  })

  test('cytat ze znaku wiekszosci', () => {
    expect(naHtml('> uwaga')).toContain('<blockquote>uwaga</blockquote>')
  })

  test('znaki HTML sa uciekane, zeby podglad nie wykonal kodu', () => {
    const html = naHtml('<img src=x onerror=alert(1)>')
    expect(html).not.toContain('<img')
    expect(html).toContain('&lt;img')
  })

  test('pusty tekst daje pusty wynik', () => {
    expect(naHtml('')).toBe('')
  })
})
```

- [ ] **Krok 2: Uruchom testy — muszą zawieść**

```bash
npm test
```

- [ ] **Krok 3: Napisz `src/renderer/podglad.js`**

```javascript
// Zamiana znacznikow WhatsApp na HTML PODGLADU. Nie jest to renderer WhatsAppa —
// sluzy wylacznie temu, zeby operator widzial, jak tekst ulozy sie w czacie.

function uciekniejHtml(tekst) {
  return String(tekst)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
}

export function naHtml(tekst) {
  const zrodlo = String(tekst ?? '')
  if (!zrodlo) return ''

  const linie = zrodlo.split('\n').map((linia) => {
    const bezpieczna = uciekniejHtml(linia)

    if (/^&gt;\s?/.test(bezpieczna)) {
      return `<blockquote>${ozdob(bezpieczna.replace(/^&gt;\s?/, ''))}</blockquote>`
    }
    if (/^[-*]\s+/.test(bezpieczna)) {
      return `<li>${ozdob(bezpieczna.replace(/^[-*]\s+/, ''))}</li>`
    }
    if (/^\d+\.\s+/.test(bezpieczna)) {
      return `<li>${ozdob(bezpieczna.replace(/^\d+\.\s+/, ''))}</li>`
    }
    return `<p>${ozdob(bezpieczna)}</p>`
  })

  return linie.join('')
}

function ozdob(fragment) {
  return fragment
    .replace(/```([^`]+)```/g, '<code>$1</code>')
    .replace(/`([^`]+)`/g, '<code>$1</code>')
    .replace(/\*([^*\n]+)\*/g, '<strong>$1</strong>')
    .replace(/_([^_\n]+)_/g, '<em>$1</em>')
    .replace(/~([^~\n]+)~/g, '<del>$1</del>')
}
```

- [ ] **Krok 4: Uruchom testy — muszą przejść**

```bash
npm test
```

Oczekiwane: PASS, 42 testy.

- [ ] **Krok 5: Dodaj okno edytora do `src/renderer/index.html`**

Wstaw przed znacznikiem `<script>`:

```html
<dialog id="okno-edytora">
  <h2>Makro</h2>
  <label>Nazwa <input id="edytor-nazwa" /></label>
  <div id="pasek-formatowania">
    <button type="button" data-znacznik="*">Pogrubienie</button>
    <button type="button" data-znacznik="_">Kursywa</button>
    <button type="button" data-znacznik="~">Przekreslenie</button>
    <button type="button" data-prefiks="- ">Lista</button>
    <button type="button" data-prefiks="1. ">Numeracja</button>
    <button type="button" data-prefiks="&gt; ">Cytat</button>
  </div>
  <label>Tresc <textarea id="edytor-tekst" rows="8"></textarea></label>
  <p>Podglad:</p>
  <div id="edytor-podglad"></div>
  <menu>
    <button id="anuluj-makro" value="anuluj">Anuluj</button>
    <button id="zapisz-makro" value="zapisz">Zapisz</button>
  </menu>
</dialog>
```

- [ ] **Krok 6: Dodaj style do `src/renderer/styl.css`**

```css
#pasek-formatowania { display: flex; gap: 4px; flex-wrap: wrap; margin: 8px 0; }
#pasek-formatowania button { padding: 4px 10px; font-size: 12px; }
#edytor-podglad { background: #0b141a; border-radius: 6px; padding: 10px; min-height: 60px; max-height: 30vh; overflow: auto; }
#edytor-podglad p { margin: 0 0 6px; }
#edytor-podglad li { margin-left: 18px; }
#edytor-podglad blockquote { margin: 0 0 6px; padding-left: 10px; border-left: 3px solid var(--akcent); color: #8696a0; }
#edytor-podglad code { background: #202c33; padding: 1px 4px; border-radius: 4px; font-family: Consolas, monospace; }
```

- [ ] **Krok 7: Dodaj obsługę edytora do `src/renderer/renderer.js`**

```javascript
import { naHtml } from './podglad.js'

const oknoEdytora = document.getElementById('okno-edytora')
const edytorNazwa = document.getElementById('edytor-nazwa')
const edytorTekst = document.getElementById('edytor-tekst')
const edytorPodglad = document.getElementById('edytor-podglad')

function odswiezPodglad() {
  edytorPodglad.innerHTML = naHtml(edytorTekst.value)
}

edytorTekst.addEventListener('input', odswiezPodglad)

document.getElementById('pasek-formatowania').addEventListener('click', (zdarzenie) => {
  const przycisk = zdarzenie.target.closest('button')
  if (!przycisk) return
  const poczatek = edytorTekst.selectionStart
  const koniec = edytorTekst.selectionEnd
  const tresc = edytorTekst.value

  if (przycisk.dataset.znacznik) {
    const znak = przycisk.dataset.znacznik
    const zaznaczone = tresc.slice(poczatek, koniec) || 'tekst'
    edytorTekst.value = tresc.slice(0, poczatek) + znak + zaznaczone + znak + tresc.slice(koniec)
  } else if (przycisk.dataset.prefiks) {
    const prefiks = przycisk.dataset.prefiks.replace('&gt;', '>')
    const poczatekLinii = tresc.lastIndexOf('\n', poczatek - 1) + 1
    edytorTekst.value = tresc.slice(0, poczatekLinii) + prefiks + tresc.slice(poczatekLinii)
  }
  edytorTekst.focus()
  odswiezPodglad()
})

document.getElementById('nowe-makro').addEventListener('click', (zdarzenie) => {
  zdarzenie.preventDefault()
  oknoMakr.close()
  edytorNazwa.value = ''
  edytorTekst.value = ''
  odswiezPodglad()
  oknoEdytora.showModal()
})

document.getElementById('anuluj-makro').addEventListener('click', (zdarzenie) => {
  zdarzenie.preventDefault()
  oknoEdytora.close()
})

document.getElementById('zapisz-makro').addEventListener('click', async (zdarzenie) => {
  zdarzenie.preventDefault()
  const wynik = await window.mostHub.zapiszMakro({
    nazwa: edytorNazwa.value,
    tekst: edytorTekst.value,
  })
  if (!wynik.ok) {
    alert(wynik.bledy.join('; '))
    return
  }
  oknoEdytora.close()
})
```

Uwaga: `renderer.js` musi być ładowany jako moduł (`<script type="module">` — już tak jest), inaczej `import` nie zadziała.

- [ ] **Krok 8: Sprawdź ręcznie pełną ścieżkę**

```bash
npm start
```

`Ctrl+;` → „Nowe makro" → wpisz nazwę i treść z listą punktowaną → podgląd pokazuje punkty → zapisz → `Ctrl+;` → kliknij makro → tekst ląduje w polu wiadomości WhatsAppa i renderuje się jako lista.

- [ ] **Krok 9: Commit**

```bash
git add src/renderer tests/podglad.test.js
git commit -m "feat: edytor makra z paskiem formatowania i podgladem"
```

---

## PRZEGLĄD PO FAZIE B

Zatrzymaj się. Nie zaczynaj fazy C przed rozmową z operatorem.

**Wykonawca uruchamia i raportuje:**

```bash
npm test && npm run test:e2e && git log --oneline -6 && git status --short
```

**Operator sprawdza ręcznie:**

1. `Ctrl+;` otwiera panel makr z dowolnej zakładki
2. utworzenie makra z listą punktowaną i pogrubieniem — podgląd zgadza się z zamiarem
3. wstawienie makra do czatu WhatsApp — **tekst renderuje się jako lista, nie jako gwiazdki**
4. **wiadomość NIE wysyła się sama** — kursor stoi w polu, Enter należy do operatora
5. wyszukiwarka znajduje makro po fragmencie treści, nie tylko po nazwie

**Kryterium przejścia dalej:** punkty 3 i 4. Punkt 4 to reguła 7.1 specu — jeśli cokolwiek wysyła się samo, zatrzymaj projekt i zgłoś to jako usterkę krytyczną.

---

# FAZA C — Załączniki i dystrybucja

Cel fazy: makro może nieść PDF i mp4, a całość działa jako przenośny plik `.exe` z autostartem.

---

### Zadanie C1: Schowek plikowy przez PowerShell 5.1

**Pliki:**
- Utwórz: `src/glowny/schowek-pliku.js`
- Test: `tests/schowek-pliku.test.js`

**Interfejsy:**
- Konsumuje: nic
- Produkuje:
  - `cytujPS(tekst)` → `string`
  - `zbudujPolecenie(sciezka)` → `{plik: string, argumenty: string[]}`
  - `async ustawPlikWSchowku(sciezka, uruchom)` → `Promise<void>`

- [ ] **Krok 1: Napisz testy**

Plik `tests/schowek-pliku.test.js`:

```javascript
import { describe, test, expect } from 'vitest'
import { cytujPS, zbudujPolecenie, ustawPlikWSchowku } from '../src/glowny/schowek-pliku.js'

describe('cytujPS', () => {
  test('otacza apostrofami', () => {
    expect(cytujPS('C:\\pliki\\a.pdf')).toBe("'C:\\pliki\\a.pdf'")
  })

  test('podwaja apostrofy — zabezpieczenie przed wstrzyknieciem', () => {
    expect(cytujPS("C:\\pliki\\o'brien.pdf")).toBe("'C:\\pliki\\o''brien.pdf'")
  })
})

describe('zbudujPolecenie', () => {
  test('wola powershell.exe, nigdy pwsh', () => {
    const { plik } = zbudujPolecenie('C:\\a.pdf')
    expect(plik).toBe('powershell.exe')
  })

  test('uzywa -LiteralPath, zeby nawiasy kwadratowe nie byly wieloznacznikami', () => {
    const { argumenty } = zbudujPolecenie('C:\\podrecznik [PL].pdf')
    const polecenie = argumenty.join(' ')
    expect(polecenie).toContain('-LiteralPath')
    expect(polecenie).toContain('podrecznik [PL].pdf')
  })

  test('przekazuje -NoProfile i -STA', () => {
    const { argumenty } = zbudujPolecenie('C:\\a.pdf')
    expect(argumenty).toContain('-NoProfile')
    expect(argumenty).toContain('-STA')
  })
})

describe('ustawPlikWSchowku', () => {
  test('uruchamia zbudowane polecenie', async () => {
    const wywolania = []
    await ustawPlikWSchowku('C:\\a.pdf', async (plik, argumenty) => {
      wywolania.push({ plik, argumenty })
    })
    expect(wywolania).toHaveLength(1)
    expect(wywolania[0].plik).toBe('powershell.exe')
  })

  test('pusta sciezka nie uruchamia niczego', async () => {
    const wywolania = []
    await ustawPlikWSchowku('', async () => wywolania.push(1))
    expect(wywolania).toHaveLength(0)
  })
})
```

- [ ] **Krok 2: Uruchom testy — muszą zawieść**

```bash
npm test
```

- [ ] **Krok 3: Napisz `src/glowny/schowek-pliku.js`**

```javascript
import { execFile } from 'node:child_process'
import { promisify } from 'node:util'

const domyslneUruchom = promisify(execFile)

// Set-Clipboard -LiteralPath istnieje TYLKO w Windows PowerShell 5.1.
// PowerShell 7 (pwsh) tego parametru nie ma — stad jawne powershell.exe.
// Zweryfikowane empirycznie w etapie 0 specu (2026-08-23).
export function cytujPS(tekst) {
  return `'${String(tekst).replace(/'/g, "''")}'`
}

export function zbudujPolecenie(sciezka) {
  return {
    plik: 'powershell.exe',
    argumenty: ['-NoProfile', '-STA', '-Command', `Set-Clipboard -LiteralPath ${cytujPS(sciezka)}`],
  }
}

export async function ustawPlikWSchowku(sciezka, uruchom = domyslneUruchom) {
  if (!sciezka) return
  const { plik, argumenty } = zbudujPolecenie(sciezka)
  await uruchom(plik, argumenty, { windowsHide: true })
}
```

- [ ] **Krok 4: Uruchom testy — muszą przejść**

```bash
npm test
```

Oczekiwane: PASS, 48 testów.

- [ ] **Krok 5: Zmierz opóźnienie na żywo**

```bash
node -e "import('./src/glowny/schowek-pliku.js').then(async m => { const t=Date.now(); await m.ustawPlikWSchowku(process.env.USERPROFILE); console.log('czas:', Date.now()-t, 'ms') })"
```

Zapisz wynik do raportu z fazy. Próg ze specu: powyżej 500 ms rozważamy moduł natywny.

- [ ] **Krok 6: Commit**

```bash
git add src/glowny/schowek-pliku.js tests/schowek-pliku.test.js
git commit -m "feat: schowek plikowy CF_HDROP przez Windows PowerShell 5.1"
```

---

### Zadanie C2: Magazyn załączników

**Pliki:**
- Modyfikuj: `src/glowny/makra.js`, `tests/makra.test.js`

**Interfejsy:**
- Konsumuje: `makra.js` z zadania B1
- Produkuje:
  - `LIMIT_ZALACZNIKA_BAJTY` → `number` (104857600)
  - `async dodajZalacznik(katalogAtt, sciezkaZrodlowa)` → `Promise<string>` (ścieżka względna `att/<uuid>-nazwa`)
  - `async usunOsierociZalaczniki(katalogAtt, makra)` → `Promise<string[]>`
  - `async zajetoscMagazynu(katalogAtt)` → `Promise<number>`

- [ ] **Krok 1: Dopisz testy do `tests/makra.test.js`**

Dodaj importy na górze pliku:

```javascript
import { mkdir, writeFile as zapiszPlik, readdir } from 'node:fs/promises'
import {
  dodajZalacznik,
  usunOsierociZalaczniki,
  zajetoscMagazynu,
  LIMIT_ZALACZNIKA_BAJTY,
} from '../src/glowny/makra.js'
```

Dodaj na końcu pliku:

```javascript
async function plikTymczasowy(katalogBazowy, nazwa, tresc = 'x') {
  const sciezka = path.join(katalogBazowy, nazwa)
  await zapiszPlik(sciezka, tresc, 'utf8')
  return sciezka
}

describe('zalaczniki', () => {
  test('dodanie kopiuje plik do magazynu i zwraca sciezke wzgledna', async () => {
    const att = path.join(katalog, 'att')
    await mkdir(att, { recursive: true })
    const zrodlo = await plikTymczasowy(katalog, 'instrukcja.pdf', 'udawany pdf')

    const wzgledna = await dodajZalacznik(att, zrodlo)

    expect(wzgledna).toMatch(/^att\/[0-9a-f-]+-instrukcja\.pdf$/)
    expect(await readdir(att)).toHaveLength(1)
  })

  test('plik ponad limit jest odrzucany z podaniem rozmiaru', async () => {
    const att = path.join(katalog, 'att')
    await mkdir(att, { recursive: true })
    const zrodlo = path.join(katalog, 'wielki.mp4')
    await zapiszPlik(zrodlo, Buffer.alloc(LIMIT_ZALACZNIKA_BAJTY + 1))

    await expect(dodajZalacznik(att, zrodlo)).rejects.toThrow(/przekracza limit/)
    expect(await readdir(att)).toHaveLength(0)
  })

  test('osierocone kopie sa usuwane, powiazane zostaja', async () => {
    const att = path.join(katalog, 'att')
    await mkdir(att, { recursive: true })
    const uzywany = await dodajZalacznik(att, await plikTymczasowy(katalog, 'uzywany.pdf'))
    await dodajZalacznik(att, await plikTymczasowy(katalog, 'sierota.pdf'))

    const usuniete = await usunOsierociZalaczniki(att, [makro({ zalaczniki: [uzywany] })])

    expect(usuniete).toHaveLength(1)
    expect(await readdir(att)).toHaveLength(1)
  })

  test('zajetosc magazynu liczy sume bajtow', async () => {
    const att = path.join(katalog, 'att')
    await mkdir(att, { recursive: true })
    await dodajZalacznik(att, await plikTymczasowy(katalog, 'a.pdf', 'xxxxx'))

    expect(await zajetoscMagazynu(att)).toBe(5)
  })
})
```

- [ ] **Krok 2: Uruchom testy — muszą zawieść**

```bash
npm test
```

- [ ] **Krok 3: Dopisz do `src/glowny/makra.js`**

Dodaj importy na górze:

```javascript
import { copyFile, stat, readdir, unlink, mkdir } from 'node:fs/promises'
import path from 'node:path'
```

Dodaj na końcu pliku:

```javascript
export const LIMIT_ZALACZNIKA_BAJTY = 100 * 1024 * 1024

export async function dodajZalacznik(katalogAtt, sciezkaZrodlowa) {
  const informacje = await stat(sciezkaZrodlowa)
  if (informacje.size > LIMIT_ZALACZNIKA_BAJTY) {
    const mb = (informacje.size / 1048576).toFixed(1)
    const limitMb = (LIMIT_ZALACZNIKA_BAJTY / 1048576).toFixed(0)
    throw new Error(`plik ${mb} MB przekracza limit ${limitMb} MB`)
  }
  await mkdir(katalogAtt, { recursive: true })
  const nazwa = `${randomUUID()}-${path.basename(sciezkaZrodlowa)}`
  await copyFile(sciezkaZrodlowa, path.join(katalogAtt, nazwa))
  return `att/${nazwa}`
}

export async function usunOsierociZalaczniki(katalogAtt, makra) {
  const uzywane = new Set(makra.flatMap((m) => m.zalaczniki ?? []).map((s) => path.basename(s)))
  const usuniete = []
  for (const nazwa of await readdir(katalogAtt)) {
    if (!uzywane.has(nazwa)) {
      await unlink(path.join(katalogAtt, nazwa))
      usuniete.push(nazwa)
    }
  }
  return usuniete
}

export async function zajetoscMagazynu(katalogAtt) {
  let suma = 0
  for (const nazwa of await readdir(katalogAtt)) {
    suma += (await stat(path.join(katalogAtt, nazwa))).size
  }
  return suma
}
```

- [ ] **Krok 4: Uruchom testy — muszą przejść**

```bash
npm test
```

Oczekiwane: PASS, 52 testy.

- [ ] **Krok 5: Commit**

```bash
git add src/glowny/makra.js tests/makra.test.js
git commit -m "feat: magazyn zalacznikow z limitem 100 MB i sprzataniem sierot"
```

---

### Zadanie C3: Załączniki w interfejsie i wstawianiu

**Pliki:**
- Modyfikuj: `src/glowny/most.js`, `src/preload/preload.js`, `src/renderer/index.html`, `src/renderer/renderer.js`

**Interfejsy:**
- Konsumuje: `schowek-pliku.js`, `makra.js`
- Produkuje w `window.mostHub`: `wybierzPlik()` → `Promise<string|null>` (ścieżka względna `att/…` albo `null`)

- [ ] **Krok 1: Rozszerz handler `makra:wstaw` w `src/glowny/most.js`**

Dodaj importy na górze pliku:

```javascript
import { dialog } from 'electron'
import { access } from 'node:fs/promises'
import { dodajZalacznik } from './makra.js'
import { ustawPlikWSchowku } from './schowek-pliku.js'
```

Zamień cały handler `makra:wstaw` na:

```javascript
  ipcMain.handle('makra:wstaw', async (_zdarzenie, idMakra) => {
    const { makra } = await wczytajMakra(plikMakr)
    const makro = makra.find((m) => m.id === idMakra)
    const widok = zarzadca.aktywny()
    if (!makro || !widok) return { ok: false, brakujace: [] }

    if (makro.tekst) wstawTekst(widok.webContents, makro.tekst, clipboard)

    // Spec sekcja 8: brak pliku w magazynie nie moze wywrocic makra —
    // tekst ma zadzialac, a interfejs ma pokazac, ktorych zalacznikow brakuje.
    const brakujace = []
    for (const wzgledna of makro.zalaczniki ?? []) {
      const pelna = path.join(katalogDanych, wzgledna)
      try {
        await access(pelna)
      } catch {
        brakujace.push(wzgledna)
        continue
      }
      await ustawPlikWSchowku(pelna)
      widok.webContents.paste()
    }
    return { ok: brakujace.length === 0, brakujace }
  })
```

- [ ] **Krok 2: Dodaj kanał wyboru pliku w `zarejestrujKanalyMakr`**

```javascript
  ipcMain.handle('pliki:wybierz', async () => {
    const wynik = await dialog.showOpenDialog({ properties: ['openFile'] })
    if (wynik.canceled || !wynik.filePaths.length) return null
    try {
      return await dodajZalacznik(path.join(katalogDanych, 'att'), wynik.filePaths[0])
    } catch (blad) {
      dialog.showErrorBox('Nie mozna dodac zalacznika', blad.message)
      return null
    }
  })
```

- [ ] **Krok 3: Dopisz metodę do `src/preload/preload.js`**

```javascript
  wybierzPlik: () => ipcRenderer.invoke('pliki:wybierz'),
```

- [ ] **Krok 4: Dodaj obsługę do edytora w `src/renderer/index.html`**

Wstaw w oknie edytora, tuż przed `<menu>`:

```html
  <p>Zalaczniki: <span id="lista-zalacznikow">brak</span></p>
  <button type="button" id="dodaj-zalacznik">Dodaj zalacznik</button>
```

- [ ] **Krok 5: Dodaj obsługę do `src/renderer/renderer.js`**

```javascript
let zalacznikiMakra = []

function odswiezZalaczniki() {
  const span = document.getElementById('lista-zalacznikow')
  span.textContent = zalacznikiMakra.length
    ? zalacznikiMakra.map((s) => s.replace(/^att\/[0-9a-f-]+-/, '')).join(', ')
    : 'brak'
}

document.getElementById('dodaj-zalacznik').addEventListener('click', async () => {
  const wzgledna = await window.mostHub.wybierzPlik()
  if (!wzgledna) return
  zalacznikiMakra.push(wzgledna)
  odswiezZalaczniki()
})
```

W handlerze `nowe-makro` dodaj przed `oknoEdytora.showModal()`:

```javascript
  zalacznikiMakra = []
  odswiezZalaczniki()
```

W handlerze `zapisz-makro` zamień wywołanie na:

```javascript
  const wynik = await window.mostHub.zapiszMakro({
    nazwa: edytorNazwa.value,
    tekst: edytorTekst.value,
    zalaczniki: zalacznikiMakra,
  })
```

- [ ] **Krok 6: Sprawdź ręcznie na realnym materiale**

```bash
npm start
```

Utwórz makro z tekstem i załącznikiem `PASSango - instalacja.pdf`, wstaw je w czacie z samym sobą. Oczekiwane: tekst w polu, potem karta dokumentu w oknie wysyłki. **Nie wysyłaj** — sprawdzasz tylko wstawienie.

- [ ] **Krok 7: Commit**

```bash
git add src/glowny/most.js src/preload/preload.js src/renderer
git commit -m "feat: zalaczniki w makrach — wybor pliku i wstawianie przez schowek"
```

---

### Zadanie C4: Przenośny plik wykonywalny

**Pliki:**
- Modyfikuj: `package.json`, `src/glowny/main.js`
- Utwórz: `build/ikona.ico`

**Interfejsy:**
- Konsumuje: całą aplikację
- Produkuje: `dist/msg-hub-0.1.0-portable.exe`

- [ ] **Krok 1: Dodaj electron-builder**

```bash
npm install --save-dev electron-builder@26.15.3
```

- [ ] **Krok 2: Dopisz konfigurację do `package.json`**

Dodaj do obiektu głównego:

```json
  "build": {
    "appId": "pl.kencki.msghub",
    "productName": "msg-hub",
    "directories": { "output": "dist", "buildResources": "build" },
    "files": ["src/**/*", "package.json"],
    "win": { "target": "portable", "icon": "build/ikona.ico" },
    "portable": { "artifactName": "msg-hub-${version}-portable.exe" }
  }
```

Dodaj do `scripts`:

```json
    "dist": "electron-builder --win portable"
```

- [ ] **Krok 3: Przygotuj ikonę**

Jeśli nie masz własnej, wygeneruj zastępczą:

```bash
mkdir -p build && powershell.exe -NoProfile -Command "Add-Type -AssemblyName System.Drawing; \$b=New-Object System.Drawing.Bitmap 256,256; \$g=[System.Drawing.Graphics]::FromImage(\$b); \$g.Clear([System.Drawing.Color]::FromArgb(47,125,91)); \$ico=[System.Drawing.Icon]::FromHandle(\$b.GetHicon()); \$fs=[System.IO.File]::Create((Join-Path (Get-Location) 'build\ikona.ico')); \$ico.Save(\$fs); \$fs.Close()"
```

- [ ] **Krok 4: Włącz autostart w `src/glowny/main.js`**

Dodaj import:

```javascript
import { wczytajUklad, zapiszUklad, ustawAutostart } from './powloka.js'
```

W `app.whenReady()`, po `utworzZasobnik()`:

```javascript
  ustawAutostart(true, app)
```

- [ ] **Krok 5: Zbuduj**

```bash
npm run dist
```

- [ ] **Krok 6: Sprawdź ARTEFAKT, nie kod powrotu**

```bash
powershell.exe -NoProfile -Command "\$f=Get-Item 'dist\msg-hub-0.1.0-portable.exe'; '{0:N1} MB' -f (\$f.Length/1MB); \$f.VersionInfo.ProductName"
```

Oczekiwane: rozmiar rzędu 80–120 MB oraz `ProductName` równy `msg-hub`. Następnie uruchom plik i potwierdź, że okno wstaje z zachowanymi kontami.

- [ ] **Krok 7: Commit**

```bash
git add package.json build src/glowny/main.js
git commit -m "build: przenosny plik wykonywalny z autostartem"
```

---

## PRZEGLĄD PO FAZIE C

**Wykonawca uruchamia i raportuje:**

```bash
npm test && npm run test:e2e && git log --oneline && git status --short
```

Plus: zmierzony czas `ustawPlikWSchowku` z zadania C1 krok 5.

**Operator sprawdza ręcznie:**

1. makro z załącznikiem PDF wstawia kartę dokumentu
2. makro z załącznikiem mp4 wstawia odtwarzacz
3. makro z tekstem i załącznikiem naraz — najpierw tekst, potem plik
4. próba dodania pliku ponad 100 MB daje czytelną odmowę
5. przenośny `.exe` startuje na czystym profilu, konta wymagają zalogowania od nowa (to poprawne — inny katalog danych)
6. autostart działa po ponownym uruchomieniu Windows

---

## Definicja ukończenia

- `npm test` oraz `npm run test:e2e` przechodzą w całości
- test izolacji sesji przechodzi — bez niego produkt nie ma sensu
- test „nie wysyła wiadomości" przechodzi — bez niego łamiemy regułę 7.1
- testy granic przechodzą — bez nich reguła 7.2 nie ma egzekucji
- trzy konta logują się i przeżywają restart aplikacji
- makro tekstowe z formatowaniem renderuje się w WhatsAppie jako lista, nie jako gwiazdki
- makro z PDF i mp4 wstawia plik do okna wysyłki
- przenośny `.exe` startuje i wstaje z autostartem
